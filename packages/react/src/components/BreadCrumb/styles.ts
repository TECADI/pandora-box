import { colors } from '@pandora-box-tecadi/design-system'
import { styled } from '../../styles'
import { Flex } from '../Flex'

export const BreadCrumbComponent = styled(Flex)`
  align-items: center;
  gap: 0.425rem;
  color: ${colors.slate400};
`
